<html>

<head>
  <title><%= title %></title>
</head>
<body>
  <h1><%= keyword %></h1>
  <% for (var photo of photos) {%>
    <img src="<%= photo.src %>" title="<%= photo.title %>">
  <% } %>
</body>

</html>
